{
  "kind": "collectionType",
  "collectionName": "user_sessions_progress",
  "info": {
    "singularName": "user-session-progress",
    "pluralName": "user-sessions-progress",
    "displayName": "Progresso na Sessão",
    "description": ""
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "assessmentRoughnessResults": {
      "type": "component",
      "repeatable": true,
      "component": "general.resultado-do-audio"
    },
    "assessmentBreathinessResults": {
      "type": "component",
      "repeatable": true,
      "component": "general.resultado-do-audio"
    },
    "trainingRoughnessResults": {
      "type": "component",
      "repeatable": true,
      "component": "general.resultado-do-audio"
    },
    "trainingBreathinessResults": {
      "type": "component",
      "repeatable": true,
      "component": "general.resultado-do-audio"
    },
    "assessmentStatus": {
      "type": "enumeration",
      "enum": ["NOT_NEEDED", "WAITING", "READY", "DONE"],
      "default": "NOT_NEEDED",
      "required": true
    },
    "trainingRoughnessStatus": {
      "type": "enumeration",
      "enum": ["NOT_NEEDED", "WAITING", "READY", "DONE"],
      "required": true,
      "default": "WAITING"
    },
    "trainingBreathinessStatus": {
      "type": "enumeration",
      "enum": ["NOT_NEEDED", "WAITING", "READY", "DONE"],
      "default": "WAITING",
      "required": true
    }
  }
}
