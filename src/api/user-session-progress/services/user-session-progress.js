'use strict';

/**
 * user-session-progress service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::user-session-progress.user-session-progress');
