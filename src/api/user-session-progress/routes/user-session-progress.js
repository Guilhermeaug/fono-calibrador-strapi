'use strict';

/**
 * user-session-progress router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::user-session-progress.user-session-progress');
